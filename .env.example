# Optional: PostgreSQL Database Connection String for persistent cloud sync (Neon, Supabase, Vercel Postgres, etc.)
# If left empty or omitted, the application seamlessly runs in offline LocalStorage mode.
DATABASE_URL="postgresql://user:password@host:port/database?sslmode=require"
